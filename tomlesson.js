/*

let coffeIsGrinding = false;

const pressGrindBeans = () => {
if (coffeIsGrinding) {
console.log( "Stopping the grind");
coffeIsGrinding = false;

} else {

console.log("Grinding is about to begin");
coffeIsGrinding = true;
}

}

pressGrindBeans();






let coffeOrder = (size, drinkType) =>{

    console.log('Order received:  ${size} ${drinktype}');
} 
takeOrder("Tall", "Latte"); 








const addUp = (num1, num2) => {
    return num1 + num2; 
}
addU
p(7,3); 
console.log (addUp(2,5)); 




const multipyByNineFifths = (celsius) => {
     return celsius + (9/5);

};

const getFahrenheit = (celsius) ==> {
     return multiplyByNineFifths(celsius) +32;
    
};

console.log("the temperature is " +getFahrenheit(15) + "F")









let orderCount = 0; 
const takeOrder = (topping) => {

    console.log('Pizza with ${topping}');
} 
orderCount ++;

TakeOrder ("pineaple");





const cashWithdrawal = (amount)





function = subOrder (top1, top2, top3, top4, top5) =>{
    console.log (we are proeparing uour sanwich ) 
}

*/